// Placeholder chat API route.
// Implemented in Step 3 (LLM provider + chat API route) per
// requirements/hotel-guest-assistant-dev-plan.md — will return a structured
// ChatApiResponse (see plan §3) instead of this placeholder.

export async function POST() {
  return Response.json({ status: 'not_implemented' }, { status: 501 });
}
